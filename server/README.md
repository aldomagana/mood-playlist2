# Server (Express)

Set environment variables in a `.env` file at the project root (or set in your environment):

SPOTIFY_CLIENT_ID=...
SPOTIFY_CLIENT_SECRET=...
SPOTIFY_REDIRECT_URI=http://localhost:3000/api/callback

Run the server in development:

```bash
cd server
npm install
npm run dev
```
