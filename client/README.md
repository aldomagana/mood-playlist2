# Client (React)

Run the client in development:

```bash
cd client
npm install
npm start
```

During development, run the server on port 3000 so the client can proxy API requests, or configure proxy in `package.json`.
